import Pagina from "@/components/Pagina";

export const metadata = {
  title: "manifiesto — pablo",
};

export default async function ManifiestoPage() {
  return <Pagina slug="manifiesto" variant="manifiesto" />;
}
